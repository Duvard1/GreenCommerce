spring.application.name=LoginService

# Configuración de la base de datos (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/greencommerce_users_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=sql_123

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Puerto de la aplicación
server.port=8082

# Configuración de seguridad JWT
jwt.secret=MiClaveJWTSecretaSegura!!1234567890
